# Legacy .env support — API keys stored here are still loaded for back-compat.
# The recommended way is to set keys via:  claude-config
#
# Format: KEY=value  (no quotes)
DEEPSEEK_API_KEY=your-deepseek-api-key-here
